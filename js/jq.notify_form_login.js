(function($) {jQuery.fn.notify_form_login = function (options) {	var default_options = {	}	options = $.extend(default_options,options);			return this.each(function() {		var notify = $("#notify_form_login");		var div = $(this);		notify.hide();		if (notify.html()!=''){			notify.wcenter();			$("#block_form").show();			notify.fadeIn("slow");		}				$("#block_form").click(function(){			$("#block_form").hide();			notify.fadeOut("fast");			$("#input_username").focus();		});				notify.click(function(){			$("#block_form").hide();			$(this).fadeOut("fast");			$("#input_username").focus();		});				$(window).keyup(function(e){			if (e.keyCode == '27') {				if (notify.css("display")!='none'){					$("#block_form").hide();					notify.fadeOut("fast");					$("#input_username").focus();				}			}		});    });}})(jQuery);